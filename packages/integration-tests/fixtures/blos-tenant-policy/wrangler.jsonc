{
  "$schema": "../../node_modules/wrangler/config-schema.json",
  "name": "blos-tenant-policy",

  // Test double for a deployment's TENANT_POLICY binding: stateless, no storage, no RPC validation
  // build. The Workshop reaches it through the harness's `services` option.
  "main": "src/blos-tenant-policy.ts",

  "compatibility_date": "2026-09-04"
}
